node_modules/
dist/
Shippable/
/data/

# Settings -> Export App State dumps (main.ts's export-app-state handler,
# written to the portable root) — a debug snapshot, not repo content.
/dts-app-state_*.txt

# Obsidian vault — Claude-maintained project notes/documentation, replacing
# most of CLAUDE.md's old bulk (see CLAUDE.md itself, now a short pointer
# into this vault). Local-only by design, not shared via git.
/notes/

# Tauri port exploration (tauri-port/) — Rust build output and the
# Tauri CLI's own npm install, both regenerable via `npx tauri dev`/`build`.
tauri-port/src-tauri/target/
tauri-port/src-tauri/gen/

# Mobile port (Capacitor/Android) — a separate, not-yet-committed subproject
# with its own node_modules + Android build tree; excluded from the desktop
# app's own git history for now (per explicit decision, 2026-09-18).
/mobile/

# Comfy Bridge (comfy-bridge/) — same conventions as the root app above:
# generated tsc/esbuild output (regenerated by `npm run build` from
# comfy-bridge/), its portable dev build, and Android build outputs.
comfy-bridge/main.js
comfy-bridge/preload.js
comfy-bridge/comfy-core.js
comfy-bridge/renderer/app.js
comfy-bridge/renderer/shared.css
comfy-bridge/mobile/www/shared.js
comfy-bridge/mobile/www/shared.css
comfy-bridge/mobile/www/app.js
comfy-bridge/Comfy Bridge.exe
comfy-bridge/LICENSE.electron.txt
comfy-bridge/LICENSES.chromium.html
comfy-bridge/chrome_100_percent.pak
comfy-bridge/chrome_200_percent.pak
comfy-bridge/d3dcompiler_47.dll
comfy-bridge/ffmpeg.dll
comfy-bridge/icudtl.dat
comfy-bridge/libEGL.dll
comfy-bridge/libGLESv2.dll
comfy-bridge/locales/
comfy-bridge/resources/
comfy-bridge/resources.pak
comfy-bridge/snapshot_blob.bin
comfy-bridge/v8_context_snapshot.bin
comfy-bridge/vk_swiftshader.dll
comfy-bridge/vk_swiftshader_icd.json
comfy-bridge/vulkan-1.dll
comfy-bridge/mobile/android/build/
comfy-bridge/mobile/android/.gradle/
comfy-bridge/mobile/android/app/build/
comfy-bridge/mobile/android/capacitor-cordova-android-plugins/build/
comfy-bridge/mobile/android/local.properties

# Generated (tsc/esbuild output of src/) — never hand-edited, regenerated by
# `npm run build`, which is wired as a `pre*` step on start/dist/dist:zip/
# dist:dir/refresh-app.
/main.js
/preload.js
/renderer/app.js
/comfy-core.js

# Root build (electron-builder --dir output, copied here by refresh-app —
# see scripts/refresh-portable-dev.js). Tied to Electron's currently-bundled
# runtime files; if a future Electron upgrade adds/removes one, check
# `git status` after the next refresh-app and add it here if needed.
/Osmium Workshop.exe
/LICENSE.electron.txt
/LICENSES.chromium.html
/README.txt
/chrome_100_percent.pak
/chrome_200_percent.pak
/d3dcompiler_47.dll
/ffmpeg.dll
/icudtl.dat
/libEGL.dll
/libGLESv2.dll
/locales/
/resources/
/resources.pak
/snapshot_blob.bin
/v8_context_snapshot.bin
/vk_swiftshader.dll
/vk_swiftshader_icd.json
/vulkan-1.dll
