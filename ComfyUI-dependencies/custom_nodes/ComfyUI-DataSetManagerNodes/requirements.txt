# Every node bundled in this pack only needs what ComfyUI/PyTorch already bring in — nothing
# extra to install. was_text_nodes/ vendors the FULL upstream WAS Node Suite file (see its own
# README.md for why), whose own requirements.txt lists ~20 heavier packages (opencv, transformers,
# rembg, ...), but none of those are needed for the five text-utility nodes actually registered
# from it. Kept as a placeholder for future needs.
